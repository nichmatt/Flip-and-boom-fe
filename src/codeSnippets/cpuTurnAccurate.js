import { randomNumber, pause, removeCardFromBoard } from "../helpers";

export const cpuTurnAccurate = ({
	board,
	setEnemyHp,
	enemyHp,
	setTurn,
	setHp,
	hp,
	setBoard,
  boardShownTrueOnly
}) => {

};
